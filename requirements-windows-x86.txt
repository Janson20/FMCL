# x86（32 位 Windows）构建依赖 —— 显式清单（勿引用 requirements.txt）
#
# 与 requirements.txt 的差异：
# - sentencepiece：无 win32 轮子，且源码构建在 setup.py 中明确拒绝 Win32，
#   必须从清单移除（语音输入在 x86 构建自动降级不可用）
# - onnxruntime-directml：仅提供 win_amd64 轮子，不安装（见 requirements-windows.txt）
# - numpy：2.x 起放弃 win32 → 固定 1.26.4（cp311 win32 轮子）
# - psutil：6.x 起放弃 win32 → 固定 5.9.8（abi3，兼容 cp311）
# - cryptography：42.x 起仍提供 abi3 win32 轮子，保持最新 46.0.0
#   （避免历史版本安全告警：Bleichenbacher 时间侧信道、PKCS12 空指针等）
#
# 依赖增减时需同步维护本文件与 requirements.txt / requirements-windows.txt

cryptography==46.0.0
minecraft-forgepy>=1.0
requests>=2.28.0
logzero>=1.7.0
tqdm>=4.64.0
keyboard>=0.13.5
customtkinter>=5.2.0
tkinterdnd2>=0.4.0
orjson>=3.9.0
aiohttp>=3.9.0
rarfile>=4.1
pygame>=2.5.0
mutagen>=1.46
markdown>=3.4
tkinterweb>=3.0
tkhtmlview>=0.3
psutil==5.9.8
nvidia-ml-py>=12.535
gpu-detector>=0.3.0
pillow>=10.0.0
qrcode>=8.0
sounddevice>=0.4.6
numpy==1.26.4
winsdk>=1.0.0b10
