# x86（32 位 Windows）构建依赖
#
# 部分包已停止提供 win32 轮子，需固定到仍提供 win32 轮子的版本：
# - numpy: 2.x 起放弃 win32 → 固定 1.26.4
# - psutil: 6.x 起放弃 win32 → 固定 5.9.8（abi3，兼容 cp311）
# - cryptography: 保持最新（46.x 仍提供 cp311-abi3-win32 轮子），
#   避免历史版本的安全告警（Bleichenbacher 时间侧信道等）
#
# 未安装项（无 win32 轮子，功能自动降级）：
# - onnxruntime-directml / sentencepiece：语音输入在 x86 构建不可用
-r requirements.txt
numpy==1.26.4
psutil==5.9.8
cryptography==46.0.0
